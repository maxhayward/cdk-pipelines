export enum EnvironmentTag {
  PROD = "production",
  STAGE = "stage",
  DEV = "dev",
}

export enum ServiceNameTag {
  MATTTHOMAS = "mattthomas",
}

export enum ServiceTypeTag {
  NETWORK = "network",
  DATABASE = "database",
  AUTH = "auth",
  COMPUTE = "compute",
  STORAGE = "storage",
}
